//
//  CameraColors.swift
//  Frame-iOS
//
//  Camera UX colors are intentionally non-adaptive (universal viewfinder
//  conventions) and not part of the themable FrameTheme palette.
//

import SwiftUI

enum CameraColors {
    static let overlay = Color.black.opacity(0.8)
    static let stroke  = Color.white.opacity(0.95)
}
